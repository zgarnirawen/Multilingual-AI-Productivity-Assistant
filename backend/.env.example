# Connector mode: stub (default) keeps the existing in-memory modules for local tests.
# Set to api when the real Agenda and To-Do services are available.
PRODUCTIVITY_CONNECTOR=stub

# Required only when PRODUCTIVITY_CONNECTOR=api
TODO_API_URL=http://localhost:4001/
AGENDA_API_URL=http://localhost:4002/
PRODUCTIVITY_API_TOKEN=
